import 'package:flutter/material.dart';

/// to be deleted when app theming is done
class DetailStyles {}

/// to be deleted when app theming is done
class DetailColors {
  static const mutedText = Color(0xFF71717A);
  static const zinc50 = Color(0xFFFAFAFA);
  static const dark1 = Color(0xFF525252);
  static const dark2 = Color(0xFF0A0A0A);
}
