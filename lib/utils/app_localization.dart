
import 'dart:ui';

class AppLocalization {

  static const Locale engLocale = Locale('en', 'US');
  static const Locale deLocale = Locale('de', 'DE');

  static const List<Locale> allLocales = [
    engLocale,deLocale
  ];

  static const String localePath = 'assets/translations';
}